# Python Demo Project

This is a simple Python demo project used to test the **CICD Pipeline Generator GitHub App**.  
It includes:

- A sample Python module (`src/calculator.py`)
- Unit tests (`tests/test_calculator.py`)
- Requirements file
- CI workflow (GitHub Actions)

update

